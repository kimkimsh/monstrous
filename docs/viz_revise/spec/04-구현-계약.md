# 04. 구현 계약

이 문서를 어기면 개편이 아니라 새 도구가 된다.

---

## 1. 보존 경계 — 절대 규칙

### 1-1. diff 0줄이어야 하는 구간

| 구간 | 줄 |
|---|---|
| 출력 계약·트랙 판정 | `539 – 780` |
| 실패 분류 | `782 – 797` |
| 파일 수집·병합 | `799 – 960` |
| 모델 조립 | `962 – 1278` |
| i18n 엔진 | `1705 – 1888` |
| 로드 파이프라인 | `3369 – 3504` |
| 내보내기 | `3711 – 3758` |

**함수 이름·시그니처·반환 구조·문자열까지 전부.** 특히:

- `inspectLetter` / `inspectBoxed` / `inspectAnswer` 의 `checks[].k` · `.d` 한국어 문자열
- `phasesOf` 의 위상 이름 `"플래닝"` · `"승인 대기"` · `"실행"` · `"집계"`
- `verdictOf` 의 `label` 문자열
- `RULES[].why` 문자열
- `CONTRACT[].label` · `.spec` 문자열

이것들은 UI 문구처럼 보이지만 **사실상 열거형이거나 판정 로직의 일부**다. 번역은 `EN` 사전이 담당한다.

### 1-2. 검증 — 한 줄로 끝난다

```bash
# 개편 전 기준선을 뜬 뒤
diff docs/viz_revise/research/baseline-normalized.json ~/Downloads/monstrous-normalized.json
```

`btn-json` 이 내보내는 것은 실행 id·워크스페이스·계약·`gradable`·`blocker`·토큰·위상·태스크별 검사 결과 전부다. **같으면 로직을 안 건드린 것이다.**

### 1-3. 유지해야 하는 DOM id

렌더를 새로 써도 이 id 들은 남아야 한다. 보존 코드가 직접 참조한다.

```
#drop  #derr  #dfiles  #btn-dir  #btn-file  #in-dir  #in-file  #lang  #lang2
#pane  #pane-all  #pane-cmp  #list  #q  #f-status  #f-ws  #splitter  #btn-side
#src  #btn-theme  #btn-contrast  #btn-present  #btn-export  #btn-json  #btn-print  #btn-load
```

계기 id (`#s-exec` 등)는 새 구조로 바뀌므로 `renderHeader` 를 새로 쓴다. **`renderHeader` 는 보존 구간이 아니다** (1890, 렌더 계층).

---

## 2. 코드 규약

### 2-1. 계산과 표현의 분리

렌더 함수는 **계산하지 않는다.** 값은 P0에서 추출한 순수 함수가 준다.

```js
// ❌ 현행 — 임계값이 마크업 안에
`<td class="n"${wait&&wait>10000?' style="color:var(--warn)"':""}>`

// ✅ 새
const WAIT_WARN_MS=10000;
const cls = wait>WAIT_WARN_MS ? "n warn" : "n";
`<td class="${cls}">`
```

**인라인 `style="…"` 은 20개 이하.** 남는 것은 CSS 변수 전달 전용이다 (`--c`, `--w`, `--on`, `--cells`).

### 2-2. 색 리터럴 금지

```bash
# 검사 — CSS 변수 정의 블록 밖에 hex 가 있으면 안 된다
grep -n '#[0-9a-fA-F]\{6\}' src/app.html | grep -v '^\s*[0-9]*:\s*--'
```

예외: `--ink` 를 SVG 내보내기에서 실제 색으로 굳히는 코드 (3730-3736).

### 2-3. 컴포넌트 함수

각 컴포넌트는 문자열을 돌려주는 함수 하나다.

```js
const stamp=v=>`<span class="stamp" data-v="${v.k||"hold"}"><b>${t(v.label)}</b><em>${STAMP_EN[v.label]||""}</em></span>`;
const gauge=(on,cells,c,dim=0)=>`<div class="gauge${dim?" has-dim":""}" role="img" aria-label="…"
  style="--cells:${cells};--on:${on};--dim:${dim};--c:${c}"></div>`;
const trk =k=>`<span class="trk" data-t="${k}">${GLYPH[k]}${TRACK[k].label}</span>`;
const ava =a=>…;
```

**같은 컴포넌트가 여러 화면에서 재사용된다.** 도장이 목록·개요·장부·비교에서 같은 함수다.

### 2-4. 클래스 이름

| 접두 | 무엇 |
|---|---|
| `hud-` | 상단 바 |
| `row-` | 목록 행 |
| `sm-` | 스쿼드 맵 (현행 유지) |
| `rp-` | 재생 (현행 유지) |
| `chk-` | 검사 항목 |
| `stat-` | 계기 |
| 접두 없음 | `panel` `q` `fn` `gauge` `stamp` `trk` `ava` `stack` — 전역 컴포넌트 |

**CSS 선택자 특이도를 낮게 유지한다.** `.panel .stat-v` 정도가 상한. `.section` 과 `.cta` 처럼 서로 상쇄하는 규칙을 만들지 않는다.

---

## 3. i18n

### 3-1. 엔진은 그대로

`t` · `tn` · `paraKey` · `translateParas` · `translateDOM` · `rebuildLong` · `i18nAudit` (1705-1882). 4단계 탐색(문단 → 텍스트 노드 → 자리표시자 → 정규식)이 그대로다.

### 3-2. 사전 키는 전면 교체

새 화면 문구에 맞춰 `EN` 을 다시 쓴다. 규칙은 현행 그대로:

- **키가 한국어 원문.** 사전에 없으면 원문이 그대로 나온다
- **데이터는 번역하지 않는다.** `.scroll` 과 `[data-raw]` 안쪽은 순회에서 제외
- `PHASE_EN` 은 별도 사전 — `"실행"` 이 카운트의 `실행` 과 글자가 같지만 뜻이 다르다

### 3-3. 새로 필요한 것

| 항목 | 처리 |
|---|---|
| 도장 영문 라벨 | `STAMP_EN` 신설. 사전이 아니라 **상시 병기**되는 값이라 `LANG` 과 무관 |
| HUD 라벨 (`CREDITS` 등) | 영문 고정. 번역 안 함 |
| `1칸 = 100k tok` | 자리표시자 키 `"1칸 = {0} tok"` |
| 스택 막대 하단 라벨 | 일반 키 |

### 3-4. 검사

```js
i18nAudit()   // 영어 모드에서 0이 나올 때까지
```

세 워크스페이스 × 3모드 × 6탭 × 각주 펼침 상태 전부.

---

## 4. 접근성

| 항목 | 요구 |
|---|---|
| 텍스트 명암비 | **4.5:1 이상** (고대비 7:1) |
| 포커스 | `:focus-visible` 2px `--focus`, offset 2px. **예외 없이** |
| 히트 영역 | 조밀 데스크톱 40×40, 슬라이더 손잡이 24×24 |
| 색만으로 뜻 전달 | 금지. 판정=글자, 트랙=글리프+글자, 위상=글자 |
| 차트 | `role="img"` + `aria-label` 요약 |
| 목록 | `role="listbox"` / `option` / `aria-selected` |
| 탭 | `role="tab"` / `aria-selected` / `aria-controls` |
| 도움말 | `role="dialog"` + `aria-modal` + `Esc` |
| 감속 | `prefers-reduced-motion` 처리 (`02-모션-스펙.md` §10) |
| 호버 | `@media (hover:hover) and (pointer:fine)` 안에만 |

---

## 5. 빌드 검사 — `viz/tools/build.sh` 에 추가

현행 검사 두 개(외부 요청 0 · 잔재 마커 0)를 유지하고 아래를 더한다.

```bash
fail(){ echo "FAIL: $1" >&2; exit 1; }

# ── 토큰 규율 ──
n=$(grep -o 'font-size:[0-9.]*px' src/app.html | sort -u | wc -l)
[ "$n" -le 7 ] || fail "글자 크기 $n 종 (최대 7)"

grep -oE '(padding|margin|gap):[^;]*' src/app.html | grep -oE '[0-9]+px' | sort -u \
  | grep -vE '^(0|1|2|4|8|12|16|24|32|48|64)px$' | grep . \
  && fail "4px 격자를 벗어난 간격"

grep -o 'border-radius:[^;]*' src/app.html | sort -u \
  | grep -vE 'border-radius:(0|50%)$' | grep . \
  && fail "border-radius 는 0 또는 50% 만"

n=$(grep -c 'style="' src/app.html)
[ "$n" -le 20 ] || fail "인라인 style $n 개 (최대 20)"

grep -q 'transition:all' src/app.html && fail "transition: all 금지"

n=$(grep -o 'cubic-bezier([^)]*)' src/app.html | sort -u | wc -l)
[ "$n" -le 3 ] || fail "이징 $n 종 (최대 3)"

# ── 보존 경계 ──
for r in "539,780" "782,797" "799,960" "962,1278" "1705,1888" "3369,3504" "3711,3758"; do
  a=${r%,*}; b=${r#*,}
  diff <(sed -n "${a},${b}p" .baseline/app.html) <(sed -n "${a},${b}p" src/app.html) >/dev/null \
    || fail "보존 구간 ${r} 이 변경됨"
done

# ── 명암비 ──
python3 tools/contrast.py src/app.html || fail "명암비 4.5:1 미달"
```

> **보존 경계 검사는 줄 번호로 하므로 P0 이후 줄 번호가 바뀐다.** P0 완료 시점의 파일을 `.baseline/app.html` 로 고정하고, 구간 좌표를 그때 값으로 갱신한다. 더 안전한 대안은 각 구간을 마커 주석으로 감싸는 것이다:
> ```js
> /* @preserve:begin verdict */ … /* @preserve:end verdict */
> ```
> 마커 방식을 권장한다. 줄 번호는 리팩터링 한 번에 무너진다.

### 5-1. `tools/contrast.py`

```python
#!/usr/bin/env python3
"""CSS 변수 블록에서 색을 뽑아 텍스트 토큰 × 배경 토큰 명암비를 검사한다."""
import re, sys

PAIRS = [("--fg","--bg"),("--fg","--panel"),("--fg","--panel-2"),
         ("--fg-dim","--bg"),("--fg-dim","--panel"),("--fg-dim","--panel-2"),
         ("--fg-faint","--bg"),("--fg-faint","--panel"),("--fg-faint","--panel-2"),
         ("--ember","--panel-2"),("--gold","--panel-2"),("--lime","--panel-2"),
         ("--coral","--panel-2"),("--magenta","--panel-2"),("--plum","--panel-2"),
         ("--ink","--ember"),("--ink","--gold"),("--ink","--lime"),
         ("--ink","--coral"),("--ink","--magenta"),("--ink","--plum")]
MIN = 4.5

def lin(c):
    c /= 255
    return c/12.92 if c <= 0.03928 else ((c+0.055)/1.055)**2.4

def lum(h):
    h = h.lstrip("#")
    r, g, b = (int(h[i:i+2], 16) for i in (0, 2, 4))
    return 0.2126*lin(r) + 0.7152*lin(g) + 0.0722*lin(b)

def ratio(a, b):
    la, lb = lum(a), lum(b)
    hi, lo = max(la, lb), min(la, lb)
    return (hi+0.05) / (lo+0.05)

src = open(sys.argv[1], encoding="utf-8").read()
bad = 0
# :root · [data-theme="light"] · [data-contrast="1"] 블록마다 검사
for m in re.finditer(r"(:root|html\[[^\]]+\])[^{]*\{([^}]*)\}", src):
    scope, body = m.group(1), m.group(2)
    vals = dict(re.findall(r"(--[\w-]+)\s*:\s*(#[0-9a-fA-F]{6})", body))
    if "--bg" not in vals:
        continue
    floor = 7.0 if "contrast" in scope else MIN
    for fg, bgk in PAIRS:
        if fg in vals and bgk in vals:
            r = ratio(vals[fg], vals[bgk])
            if r < floor:
                print(f"{scope}: {fg} on {bgk} = {r:.2f} (< {floor})")
                bad += 1
sys.exit(1 if bad else 0)
```

**한계를 명시한다:** 이 스크립트는 **같은 블록 안에 함께 정의된 변수만** 검사한다. `html[data-contrast="1"]` 이 `--panel-2` 를 재정의하지 않으면 그 조합은 검사되지 않는다. 그래서 각 테마 블록은 검사에 필요한 변수를 **전부 다시 선언한다** — 토큰 블록(`00-디자인-토큰.md` §1)이 그렇게 쓰여 있다.

---

## 6. 성능 예산

| 항목 | 예산 | 측정 |
|---|---|---|
| 파일 크기 | ≤ 400KB | `wc -c viz/trace-visualizer.html` |
| 첫 렌더 (109 파일) | ≤ 2s | 스톱워치 |
| 목록 렌더 1,000행 | ≤ 100ms | `performance.now()` |
| 검색 입력 응답 | ≤ 50ms | 같은 방법 |
| 재생 프레임 | 60fps, 드롭 0 | Performance 패널 8× 5초 |
| 리사이즈 재드로 | ≤ 16ms/프레임 | 디바운스 100ms |

### 6-1. 1,000행 대응

현행 `renderList()` 는 매번 전체를 `document.createElement` 로 만든다. 100건은 괜찮고 1,000건은 안 된다.

**상한 렌더 + 더 보기**를 쓴다. 가상 스크롤보다 단순하고 이 도구에는 충분하다.

```js
const PAGE=200;
function renderList({fast=false}={}){
  const vis=visible();
  const n=Math.min(vis.length, S.listShown||PAGE);
  … n 행만 그린다 …
  if(vis.length>n) 더보기 버튼 (+200)
}
```

**행 높이가 68px 고정**이므로 나중에 가상 스크롤로 바꿀 수 있다.

`j`/`k` 로 목록 끝에 닿으면 자동으로 다음 페이지를 붙인다.

### 6-2. 하지 말 것

| 금지 | 이유 |
|---|---|
| 행마다 SVG 아바타 인라인 | 1,000행 × 12 `<rect>` = 12,000 노드. **아바타는 표·맵·비교에만** |
| 행마다 `<details>` | 접기 상태 관리 비용 |
| 행마다 `ResizeObserver` | 관찰 대상은 차트 컨테이너만 |
| 부모에 CSS 변수 갱신 | 자식 전부 스타일 재계산. 재생선은 `.rp-stage` 하나에만 |
| `innerHTML` 로 전체 목록 재조립 | 200행 상한 안에서는 허용. 그 이상은 행 재사용 |

---

## 7. 파일 구조 (단일 파일 내부 순서)

```
<!doctype html>
<html lang="ko" data-theme="dark">
<head>
  <style>
    /* 1. 토큰          — 00-디자인-토큰.md §1 */
    /* 2. 리셋·기본     */
    /* 3. 레이아웃      — hud / main / side / body / pane */
    /* 4. 컴포넌트      — 01-컴포넌트-스펙.md 순서대로 */
    /* 5. 화면별        — 03-화면별-스펙.md 순서대로 */
    /* 6. 모션          — 02-모션-스펙.md */
    /* 7. 상태 모드     — present / contrast / print / 반응형 / 감속 */
  </style>
</head>
<body>
  <!-- 정적 마크업: HUD · main · 오프닝 -->
  <script>
    /* @preserve:begin contract */  출력 계약·트랙 판정
    /* @preserve:begin failure  */  실패 분류
    /* @preserve:begin collect  */  수집
    /* @preserve:begin model    */  조립
    /* -- 신규 --                */  P0 추출 순수 함수
    /* -- 신규 --                */  i18n 사전 (새 키)
    /* @preserve:begin i18n     */  i18n 엔진
    /* -- 신규 --                */  컴포넌트 함수
    /* -- 신규 --                */  렌더 계층
    /* @preserve:begin load     */  로드 파이프라인
    /* -- 신규 --                */  조작·상태 (마크업만 교체)
    /* @preserve:begin export   */  내보내기
  </script>
</body>
</html>
```

**`@preserve` 마커로 감싸면 `build.sh` 가 줄 번호 없이 검사할 수 있다.**

---

## 8. 하지 않기로 한 것

| 유혹 | 왜 안 하나 |
|---|---|
| 판정기 문구를 키로 빼기 | 로직이 바뀐다. 번역은 사전이 한다 |
| 위상 이름을 영문 열거형으로 | `paneCmp` 가 `p.k==="실행"` 으로 비교한다 |
| 프레임워크 · 번들러 | 빌드 도구 없음 · 단일 파일 |
| 차트 라이브러리 | 외부 요청 0 |
| 웹폰트 링크 | 같은 이유. 픽셀 폰트는 base64 P2 |
| 캐시·모델별 비용 화면 | 로그에 필드가 없다. 그리면 지어내는 것 |
| 실시간 모드 | 데모 리스크. 재생 모드가 그 자리 |
| 용어를 게임 어휘로 교체 | 심사위원이 화면 단어를 문서와 대조해야 한다 |
| 인접 코드 정리 | 범위 밖. 보이면 보고만 한다 |
| `viz/trace-visualizer.html` 직접 수정 | 산출물이다. `src/app.html` 을 고치고 `build.sh` 로 재생성 |
