PLANNER: this is the coding track. Create exactly one task and assign it to Coder. The task
description is the text below the ITEM line, copied character for character from the line after
it to the last line of this message, with nothing of your own added. Create no second task and
call no other tool.

If the item carries a repository-context section — sixty thousand characters of source excerpts,
each opening with a `--- path (lines A-B of N) ---` header — it will not fit. Then copy, in this
order and stopping at about thirty-two thousand characters: the closing REQUIRED OUTPUT block
first, then the issue text whole, then as many whole excerpts as the budget allows, best first.
Rank them: a path the issue names outright, then a path in a traceback the issue pastes, then an
excerpt defining a function, class or attribute the issue names, then one that calls such a
symbol. Never trim inside an excerpt and never trim the issue text — drop a whole excerpt
instead. The worker's patch has to match the file byte for byte and your paste is its only
source.

Everyone else: the ITEM line is the start of your work. What is graded is the patch, nothing
else, and a patch that does not apply is worth the same as no patch at all.

--- ITEM ---

{{TASK}}
