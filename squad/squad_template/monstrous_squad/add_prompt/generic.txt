PLANNER: this is the multiple-choice track. Create exactly one task and assign it to Solver. The
task description is the text below the ITEM line, copied character for character from the line
after it to the last line of this message, with nothing of your own added — the `Options:` block
included, because a list you did not copy has no letters to choose from. Create no second task
and call no other tool.

Everyone else: the ITEM line is the start of your work. One letter is graded, taken from the
final line; the reasoning above it is never read.

--- ITEM ---

{{TASK}}
