# 01. 검증 결과 — 무엇을 어떻게 쟀나

전부 실제로 브라우저에서 돌려 잰 값이다. 계산값이 아니다.

**측정 환경**
- Chromium (Playwright), 뷰포트 1440×900 및 1280×860
- 입력: `squad/` 를 복사해 얼린 `.regression-input/` (파일 299개 · 실행 30건 · 스쿼드 8개)
- 서버: `python3 -m http.server 8777` (일부 브라우저가 `file://` 에서 폴더 읽기를 막는다)

> `.regression-input/` 은 `.gitignore` 에 들어 있다. 저장소에 안 올라가지만 로컬에는 남겨 둔다 —
> 아래 해시들이 이 입력에 묶여 있어서, 지우면 §1-1 을 다시 재현할 수 없다.
> 다시 뜨려면 `rm -rf .regression-input && cp -R squad .regression-input`.

---

## 1. 로직 무결성 — 가장 중요한 게이트

### 1-1. JSON 내보내기 바이트 동일

기획 `03 §4-1` 이 정한 기준선이다. **같은 워크스페이스를 넣고 `JSON` 내보내기를 눌렀을 때
개편 전후의 파일이 바이트 동일해야 한다.**

| | 개편 전 `viz/trace-visualizer.html` | 개편 후 `viz_revise_program/` |
|---|---|---|
| 실행 수 | 30 | 30 |
| 읽은 파일 | 299 | 299 |
| **바이트** | **90,128** | **90,128** |
| **해시** | **2136881638** | **2136881638** |
| 판정 분포 | blocked 5 · nocontract 10 · markers 9 · ok 6 | **동일** |

**동일.** 화면을 전부 갈아엎었는데 정규화 데이터는 한 바이트도 안 바뀌었다.

> **입력을 먼저 얼려야 한다.** 처음에는 `squad/` 를 직접 넣고 쟀는데 두 번째 측정에서
> 바이트가 어긋났다. 코드 회귀인 줄 알고 뒤졌더니 **측정 사이에 `squad/test_4` 가
> 새로 생겨 있었다** — 다른 작업이 같은 폴더에 쓰고 있었다.
> 살아 있는 폴더로는 두 뷰어를 같은 조건에서 못 잰다. `.regression-input/` 으로
> 복사해 얼린 뒤 양쪽에 같은 것을 넣었다. 위 표는 얼린 입력으로 잰 값이다.

<details>
<summary>재현 방법</summary>

두 파일을 각각 열고 같은 폴더를 넣은 뒤, 콘솔에서:

```js
let captured=null;
const realCreate=URL.createObjectURL;
URL.createObjectURL=b=>{ captured=b; return "blob:stub"; };
const realClick=HTMLAnchorElement.prototype.click;
HTMLAnchorElement.prototype.click=function(){};
document.querySelector('#btn-json').click();
URL.createObjectURL=realCreate;
HTMLAnchorElement.prototype.click=realClick;
const text=await captured.text();
console.log(text.length, [...text].reduce((h,c)=>(h*31+c.charCodeAt(0))>>>0,7));
```

다운로드해서 `diff` 를 떠도 된다. 위 방법은 파일을 안 만들고 확인하는 쪽이다.
</details>

### 1-2. 판정 분포가 조사 문서의 실측과 일치

`docs/viz_revise/research/02-데이터-원천과-한계.md` §3-2 가 적어 둔 실측값과 대조했다.

측정 시점의 `squad/` 는 실행 28건이었다 (그 뒤 `test_4` 가 늘어 30건이 됐다).

| 판정 | 조사 문서 | 새 뷰어 (실행 28건 시점) |
|---|---|---|
| 호출 거부 (`blocked`) | 5 | **5** |
| 계약 미전송 (`nocontract`) | 8 | **8** |
| 답 형식 없음 (`markers`) | 9 | **9** |
| 채점 적격 | 6 | **6** |
| coding / math / generic | 4 / 22 / 2 | **4 / 22 / 2** |
| AI:GO 성공인데 부적격 | 15 | **15** |

### 1-3. 보존 구간 diff

`build.sh` 가 매 빌드마다 `@preserve` 마커 사이를 잘라 개편 전 원본에서 뜬 조각과 `diff` 한다.
일곱 구간 1,105줄 전부 **0줄 차이**.

---

## 2. 결함 해소 — 측정으로

기획 `03 §4-2` 의 목표표 그대로.

| 결함 | 목표 | 실측 | |
|---|---|---|---|
| B-1 명암비 3.25:1 | 모든 텍스트 토큰 ≥ 4.5:1 (고대비 7:1) | **최저 5.17:1** · 고대비 최저 **9.83:1** | ✅ |
| A-1 글자 크기 14종 | 7종 + 리터럴 0 | **7종 · 리터럴 0** (SVG 속성형 포함) | ✅ |
| A-2 반지름 8종 | 1종 (0) | **`border-radius:0` 만** | ✅ |
| A-3 인라인 스타일 89개 | ≤ 20 | **18개** | ✅ |
| C-1 조작부 158.3px | ≤ 100px | **97px** (56 + 41) | ✅ |
| C-2 산점도 520×250 | ≥ 340px · 전폭 | **1364 × 340** | ✅ |
| D-1 고빈도 애니메이션 | `j`/`k` 경로 0 | **`.no-anim` 적용 확인 · 다음 프레임에 해제** | ✅ |

명암비는 `tools/contrast.py` 가 네 색 블록(`:root` · light · contrast · contrast+light)을
각각 28쌍씩 검사한다. 출력: `검사한 블록 4개, 위반 0건`.

---

## 3. HUD 폭 — 겹침 없음

여러 폭에서 계기가 서로 겹치는지, 도구 줄과 부딪치는지 확인했다.

| 뷰포트 | 보이는 계기 | 겹침 | 조작부 높이 |
|---|---|---|---|
| 1920 | 실행 · ⇄ · CREDITS · TIME | 없음 | 97px |
| 1600 | 4개 전부 | 없음 | 97px |
| 1440 | 4개 전부 | 없음 | 97px |
| 1280 | 실행 · ⇄ · **CREDITS** | 없음 | 97px |

1440px 실측 예산: 워드마크 168 + 계기 719 + 도구 473 + 여백 80 = 1440.

계기는 뒤에서부터 사라진다. **CREDITS 와 환산 비율(`1칸 = 8.0k tok`)은 마지막까지 남는다** —
환산값을 화면에서 빼면 칸이 단위를 지어내는 것이 되기 때문이다.

---

## 4. 기능 보존

| 항목 | 결과 |
|---|---|
| 단축키 9개 (`j k 1-6 m / b ? Esc` + 방향키) | **전부 동작** |
| 모드 순환 `m` | exec → all → cmp → exec |
| URL 해시 왕복 | `#e=…&t=ledger` 생성·복원 확인 |
| 사이드바 폭 조절 · 더블클릭 초기화 | 동작 |
| 목록 / 격자 전환 | 동작 |
| 맵 확대(0.25~6배)·이동·6px 클릭 임계 | 동작 |
| 재생 전체·구간·스크럽·속도 1~60× | 동작. 구간 끝 자동 정지 확인 |
| 한/영 전환 | 동작. **데이터는 번역 안 됨** |
| 고대비 · 발표 · 인쇄 · 테마 | 동작 |
| SVG · JSON 내보내기 | 동작 |
| 폴더 드롭 · 파일 고르기 · 폴더 다시 고르기 | 동작 |

### 4-1. 선택 상태 공유 (신규 · traceability 축)

맵 노드를 클릭했을 때:

| 반응 | 결과 |
|---|---|
| 맵 노드 포커스 링 | ✅ 1개 |
| 레인 막대 강조 + 나머지 감쇠 | ✅ 1개 |
| 「장부 대조」 탭의 해당 응답 후보 강조 | ✅ 1개 / 후보 2개 중 |
| `Esc` 로 전부 해제 | ✅ `S.focus === null` |

설정 경로는 넷이다 — 맵 노드 · 레인 막대 · 티커 행 · 장부 응답 제목. 전부 같은 상태를 쓴다.

### 4-2. 시그니처 — 절단선이 HUD 를 흔든다

슬라이더를 끌었을 때:

| | 끌기 전 | 끌기 후 |
|---|---|---|
| HUD 게이지 `--on` | 19 | **17** |
| HUD 게이지 `--dim` | 0 | **2** (빗금으로 남음) |
| 환산 줄 | `1칸 = 8.0k tok` | `1칸 = 8.0k tok · −127.6k` |
| 산점도 잘린 점 | 0 / 28 | **21 / 28** |
| 절약 토큰 | 0 | 127.6k (전체의 82.5%) |

---

## 5. i18n

영어 모드에서 **실행 28건 × 탭 6개 × 모드 3개**, 각주를 전부 편 상태로 `i18nAudit()` 을 돌렸다.

| | 값 |
|---|---|
| 한국어가 남은 문자열 (전체) | 102 |
| 그중 **로그 데이터** | 102 |
| 그중 **UI 문구** | **0** |

로그 데이터는 번역 대상이 아니다. 그건 로그의 내용이지 UI 가 아니다.
남은 102개는 전부 에이전트 이름(`보안 리뷰어` · `백엔드 개발자`)과 태스크 제목,
요청 원문이다. 화면이 만든 문구와 붙어 한 덩어리로 잡히는 것들이라
(`보안 리뷰어: For how many real values… Runs 180.3s`) 처음에는 UI 잔존으로 보였는데,
조각을 갈라 보면 한국어 부분이 전부 로그의 값이다.

### 5-1. `aria-label` 도 번역된다

`i18nAudit()` 은 텍스트 노드만 본다. 눈에 안 보이는 `aria-label` 은 그 검사를 그냥 통과한다.
따로 확인했다.

| 요소 | 한국어 | 영어 |
|---|---|---|
| `#list` | 실행 목록 | **Run list** |
| `#help` | 단축키 | **Shortcuts** |

---

## 5-2. 누수

| 대상 | 20회 렌더 후 |
|---|---|
| `window` 의 `mousemove`/`mouseup` | **2개** (고치기 전 40개) |
| `ResizeObserver` 관찰 대상 | observe 8 · **unobserve 8** · 남은 것 **0** |

---

## 6. 성능

실행 28건을 복제해 1,000건으로 만들고 쟀다.

| 항목 | 예산 | 실측 |
|---|---|---|
| 목록 렌더 (1,000건 중 200행) | ≤ 100ms | **11.1ms** |
| 검색 입력 응답 | ≤ 50ms | **14.3ms** |
| 헤더 재계산 | — | **0.3ms** |
| 파일 크기 | ≤ 400KB | **292KB** |

목록은 200행 상한 + `더 보기` 다. 행 높이가 68px 고정이라 나중에 가상 스크롤로 바꿀 수 있다.
1,000행에서도 목록 안의 SVG 노드는 28개뿐이다 — 아바타를 행마다 인라인하지 않는다.

---

## 7. 오류

| 검사 | 결과 |
|---|---|
| 실행 28건 × 탭 6개 = **168 패널 렌더** | 예외 0 |
| 모드 3개 순회 | 예외 0 |
| 고대비·발표·테마 토글 각 2회 | 예외 0 |
| 브라우저 콘솔 | **오류 0 · 경고 0** |

---

## 8. 아직 확인하지 못한 것

정직하게 적는다.

| 항목 | 왜 못 했나 |
|---|---|
| **「판정 보류」(`blocker==="unknown"`) 상태** | 지금 저장소의 워크스페이스 28건에 이 상태가 **하나도 없다.** 코드 경로는 있고 문자열도 사전에 있지만 화면으로 확인하지 못했다 |
| 재생 60fps · 드롭 프레임 0 | Performance 패널 기록이 필요하다. 시계는 `RP.step()` 으로 검증했다 |
| 실제 프로젝터 시연 | 고대비 모드의 명암비는 계산·검사로만 확인했다 |
| Firefox · Safari | Chromium 에서만 돌렸다. 슬라이더는 `::-moz-` 규칙을 따로 써 뒀지만 눈으로 못 봤다 |
| 인쇄 결과물 | `@media print` 규칙은 넣었으나 실제 출력·PDF 를 확인하지 못했다 |
| 워크스페이스 1개만 넣은 경우 | 「스쿼드 비교」 안내 문구 경로. 코드에는 있다 |

**첫 항목이 가장 아쉽다.** 「판정 보류」는 이 도구의 4분류 중 하나이고,
"모르는 것을 불합격으로 적지 않는다"는 원칙이 눈에 보이는 자리다.
계약이 불명한 실행이 들어오는 워크스페이스가 생기면 그때 확인해야 한다.
